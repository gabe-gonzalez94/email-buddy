# Set to "true" to use the mock generator (no backend needed)
EXPO_PUBLIC_USE_MOCK=true

# Backend API URL (override for physical devices or custom deployments)
# iOS simulator / web: http://localhost:3001 (default)
# Android emulator:    http://10.0.2.2:3001  (auto-detected)
# Physical device:     http://<your-local-ip>:3001
# EXPO_PUBLIC_API_URL=http://localhost:3001
